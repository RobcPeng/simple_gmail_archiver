# GmailVault Configuration

# Cloudflare R2 Storage (for .eml file archival)
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=emails

# Sync interval in minutes (default: 60)
SYNC_INTERVAL_MINUTES=3600

# Server
PORT=8080
HOST=0.0.0.0
